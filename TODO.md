# TODO for Reimplementing Core App Logic

1. Reimplement Register.tsx - Keep as is (matches spec) ✓
2. Reimplement OTP.tsx - Keep as is (matches spec) ✓
3. Reimplement YearlySpend.tsx - Keep as is (matches spec) ✓
4. Reimplement Dashboard.tsx - Add yearlySpend state, load in useEffect, calculate total spent from dailySpends ✓
5. Reimplement DailySpend.tsx - Always return JSX, change button text to "Add Spend", ensure conditional appearance ✓
6. Reimplement DemoEntry.tsx - Keep as is (matches spec) ✓
